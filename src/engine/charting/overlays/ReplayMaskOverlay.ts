/**
 * ReplayMaskOverlay.ts
 * @deprecated Replaced by ReplayMaskIndicator (Custom Indicator API) in src/engine/charting/indicators/ReplayMaskIndicator.ts
 */

export const REPLAY_MASK_OVERLAY_NAME = 'replayMaskOverlay';

export function registerReplayMaskOverlay(): void {
  // Obsolete: Replay Mask is now rendered via ReplayMaskIndicator.
}
